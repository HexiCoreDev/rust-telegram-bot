// InputSticker is defined alongside MaskPosition and StickerSet in sticker.rs,
// since they are tightly coupled types. Re-export it here for convenient import
// via this module path.
pub use crate::types::files::sticker::InputSticker;
